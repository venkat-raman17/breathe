# Expo + React Native + Metro work most reliably with a flat (hoisted) node_modules.
# pnpm's default symlinked layout breaks Metro/native module resolution, so opt into hoisted.
# See https://docs.expo.dev/guides/using-pnpm/
node-linker=hoisted
